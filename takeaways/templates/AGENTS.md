# <Project name> agent notes

- <The most important domain rule>, because <reason>.
- <Use this helper>; never call <the look-alike to avoid> from new code (<ticket>).
- Run `<test command>`, `<lint command>` and `<type-check or build command>` before calling a task done.
- New behavior ships with a test in the same change.
- When a requirement is ambiguous, list the options and ask before implementing.

`.github/copilot-instructions.md` is the source of truth for these conventions; keep this file in sync with it.
