# Run A against run B

A place to record the two runs from segment 4.5 of `DEMO_SCRIPT.md`, if you want to keep
a record. Fill in as much or as little as you like.

Once filled in, this file describes what the agent got wrong. It lives in `demo-kit`, outside
the repository, so an agent never reads it.

The correct totals are 3460 for 247100 cents and 5000 for 1000000 cents, using
`price_with_policy`.

`../demo-kit/run-stats.py` prints most of run A's fields after `/exit` (run it from the
repository folder before the live reset), and `../demo-kit/run-stats.py --vscode` prints run
B's from the saved VS Code chat session (run it before the 4.7 reset). See segment 4.5 in
`DEMO_SCRIPT.md`.

## Today's live run

Date:

### Run A: one prompt in Copilot CLI

- Model:
- Files read before the first edit:
- Totals for 247100 and 1000000 cents:
- Tests added for both examples:
- Full suite green on the first attempt:
- Architecture test green:
- Checks run:
- Legacy helper (`calculate_fee`) avoided:
- Files touched:
- Turns:
- Tool calls:
- Tokens:
- AI credits:
- Notes:

### Run B: the Plan agent in VS Code

- Helper named in the plan:
- Clarifying question asked:
- Model:
- Files read before the first edit:
- Totals for 247100 and 1000000 cents:
- Tests added for both examples:
- Full suite green on the first attempt:
- Architecture test green:
- Checks run:
- Legacy helper (`calculate_fee`) avoided:
- Files touched:
- Turns:
- Tool calls:
- Tokens:
- AI credits:
- Notes:

## Rehearsal tally

Copy the attempt block once for each rehearsal, then fill in the summary from the attempts.

### Summary

**Run A**

- Model:
- Attempts:
- Attempts that failed the guard tests:
- First-attempt green rate:
- Median files read before the first edit:
- Most common totals:
- Median turns and tool calls:
- Median tokens:
- Median AI credits:

**Run B**

- Model:
- Attempts:
- Attempts that failed the guard tests:
- First-attempt green rate:
- Median files read before the first edit:
- Most common totals:
- Median turns and tool calls:
- Median tokens:
- Median AI credits:

### Attempt 1

Date:

**Run A**

- Totals:
- Failed the guard tests:
- Green on the first attempt:
- Files read before the first edit:
- Turns and tool calls:
- Tokens and AI credits:

**Run B**

- Totals:
- Failed the guard tests:
- Green on the first attempt:
- Files read before the first edit:
- Turns and tool calls:
- Tokens and AI credits:
